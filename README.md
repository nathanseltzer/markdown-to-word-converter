# Markdown to Word Converter

Paste Markdown or upload a `.md` file and download a Word `.docx`. Free, no sign-up.

Built by [ViewMarkdown.com](https://viewmarkdown.com), a free online Markdown viewer, editor and converter.

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```
